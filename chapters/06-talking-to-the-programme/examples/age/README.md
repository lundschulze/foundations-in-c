# Age

This programme displays and flushes a prompt, asks `scanf` to convert one decimal integer and uses `age` only after the conversion succeeds.

```bash
make age
./age
```

Enter `41`. The complete interaction is:

```text
Enter your age: 41
Next year you will be 42.
```

Try a word as a separate run and observe the non-zero exit status.

