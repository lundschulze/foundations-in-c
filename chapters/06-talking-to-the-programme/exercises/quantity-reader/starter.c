#include <stdio.h>

int main(void)
{
    /* Declare quantity. */
    /* Display and flush a prompt. */
    /* Read one decimal integer and validate both conversion and range. */
    /* Print the accepted quantity. */

    return 0;
}
