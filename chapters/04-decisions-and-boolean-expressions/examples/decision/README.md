# Decision

This example selects one of two branches by comparing `answer` with `42`.

```bash
make decision
./decision
```

Expected output:

```text
The computer appears satisfied.
```

Change `answer` to `41`, predict the result, then rebuild and run the programme again.

