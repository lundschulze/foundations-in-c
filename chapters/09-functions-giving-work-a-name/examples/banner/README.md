# Banner

`print_separator` performs one named piece of work and can be called repeatedly.

```bash
make banner
./banner
```

Expected output:

```text
------------------------
Journey control
------------------------
```

The first `void` says the helper returns no value. The second, inside `(void)`, says it accepts no arguments.

