# Exercise — Status display

Complete `starter.c` with `print_separator`, `print_status` and `main`. Each helper must take no arguments, return no value and perform one clearly named action.

```bash
make starter
./starter
```

Compare your work with one possible solution:

```bash
make answer
./answer
```

Expected answer output:

```text
------------------------
System status: ready.
------------------------
```

