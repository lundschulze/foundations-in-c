# Stop with `break`

When `value` reaches `4`, `break` leaves the loop before that value is printed.

```bash
make break
./break
```

Expected output:

```text
1
2
3
```

Use a clear loop condition when it can state the stopping rule directly. Use `break` when the reason to stop is discovered naturally during a pass.

