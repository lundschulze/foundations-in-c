# At least one pass

A `do while` loop runs its body before checking the condition.

```bash
make do_while
./do_while
```

Expected output:

```text
Choice: 2
Choice: 1
```

Change the initial value to zero. The body still runs once. Notice the required semicolon after the final condition.

