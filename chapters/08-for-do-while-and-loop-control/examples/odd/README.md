# Odd values

The remainder expression identifies even values, and `continue` skips their `printf` call.

```bash
make odd
./odd
```

Expected output:

```text
1
3
5
7
```

Modify the condition and upper boundary so the programme prints only even values from 1 through 8.

