# Choice

The `switch` expression is evaluated once and execution continues at the matching `case` label.

```bash
make choice
./choice
```

Expected output:

```text
Navigation selected.
```

Test `choice` with `1`, `3` and `9`. Each ordinary case ends with `break` so execution does not fall through.

