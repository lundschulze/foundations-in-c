# Fare

The conditional operator chooses one of two values for the initialisation of `fare`.

```bash
make fare
./fare
```

Expected output:

```text
Ticket price: 7.50
```

Change `age` to `16`. The condition becomes false and the selected fare is `15.00`.

