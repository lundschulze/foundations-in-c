# Rating

A `switch` may select among character constants because `char` is an integer type.

```bash
make rating
./rating
```

Expected output:

```text
Strong result.
```

Try `'A'`, `'C'` and an unrecognised character. Single quotes form a character constant; double quotes form a string literal.

