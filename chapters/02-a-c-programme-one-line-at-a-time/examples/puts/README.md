# Using `puts`

`puts` writes a fixed string followed by a newline.

Build and run the example:

```bash
make puts
./puts
```

Expected output:

```text
Hello from C.
```

Use `puts` for fixed text. Use `printf` when the output needs formatted values.
