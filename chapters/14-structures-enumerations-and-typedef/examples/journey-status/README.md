# Journey status

This example uses an enumeration to represent one state from a named set and a `typedef` alias for a record.

```bash
make journey-status
./journey-status
```

Expected output:

```text
Priya: Ready to depart.
```
