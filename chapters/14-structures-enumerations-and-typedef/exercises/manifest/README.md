# Exercise — Make a manifest

Complete `starter.c` by creating an array of at least three journey records. Derive the array count and use a loop and `switch`-based status conversion to print every member.

```bash
make starter
./starter
make answer
./answer
```

Add a fourth record. The loop boundary must not need editing.
