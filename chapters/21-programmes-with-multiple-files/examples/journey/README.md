# Journey

`journey.h` states the public contract, `journey.c` supplies its definitions, and `main.c` uses only the interface.

```bash
make journey
./journey
```

Expected output:

```text
Destination: Aurora Station
Passengers: 3
```
