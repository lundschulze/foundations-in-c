# Print readings

An array argument does not carry its element count. `print_readings` therefore receives the array and count as a pair and promises not to modify the elements.

```bash
make print_readings
./print_readings
```

The programme prints the five values on separate lines.

