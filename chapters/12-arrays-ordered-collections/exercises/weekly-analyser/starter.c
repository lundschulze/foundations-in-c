#include <stddef.h>
#include <stdio.h>

int main(void)
{
    /* Create an array of seven integer measurements. */
    /* Derive its count, print every element, calculate the total, */
    /* and find the smallest and largest values. */

    return 0;
}

