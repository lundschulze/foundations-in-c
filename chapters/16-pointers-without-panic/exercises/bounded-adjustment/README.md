# Exercise — Make a bounded adjustment

Complete `clamp` so it modifies a caller-owned integer only when it lies outside the inclusive range.

```bash
make starter
./starter
make answer
./answer
```

The answer prints `0`, `5` and `10` on separate lines.
