#include <stdio.h>

int is_in_range(int value, int minimum, int maximum);

int main(void)
{
    /* Call is_in_range with boundary, middle and outside values. */
    return 0;
}

/* Define is_in_range here. */

