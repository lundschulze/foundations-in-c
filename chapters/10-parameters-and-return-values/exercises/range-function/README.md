# Exercise — Range function

Implement `is_in_range` so it returns `1` when `value` is between the inclusive boundaries and `0` otherwise.

Test both boundaries, one middle value and the values immediately outside the range.

```bash
make starter
./starter
```

Compare your work with one possible solution:

```bash
make answer
./answer
```

Expected answer output:

```text
10: 1
15: 1
20: 1
9: 0
21: 0
```

