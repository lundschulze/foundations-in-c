# Larger value

The declaration before `main` tells the compiler the function's interface. Its definition appears after the call.

```bash
make larger
./larger
```

Expected output:

```text
Larger value: 42
```

Reverse the argument order and confirm that the function still returns the larger value.

