# Square

The argument `6` initialises the parameter `value`. The returned result initialises `result` in `main`.

```bash
make square
./square
```

Expected output:

```text
Square of 6: 36
```

Try storing `square(3) + square(4)` and predict the result before running it.

