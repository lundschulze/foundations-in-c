# Calculation

This example uses four descriptive integer variables. The fourth is initialised with the result of an expression.

```bash
make calculation
./calculation
```

Expected output:

```text
Total bags: 15
```

Multiplication is performed before addition, so the expression is evaluated as `(6 * 2) + 3`.
