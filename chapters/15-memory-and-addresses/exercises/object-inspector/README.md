# Exercise — Make an object inspector

Complete `starter.c` so it prints the value, size and address of each local object.

```bash
make starter
./starter
make answer
./answer
```

Run the answer several times. `sizeof(char) == 1` is guaranteed; exact addresses, their order and most type sizes are not.
