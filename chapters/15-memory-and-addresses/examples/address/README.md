# Address

Unary `&` obtains the address of `answer`. `printf` expects a `void *` argument for `%p`.

```bash
make address
./address
```

The value is `42`. The address is implementation-defined and may change between runs.
