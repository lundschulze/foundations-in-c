# Exercise — Make a private travel log

Write three structured text records to the disposable path already supplied. Close it, reopen it, display every line and remove only that known practice file.

```bash
make starter
make test
```

Check every resource operation and distinguish normal end of file from a read error.
