#include <stdio.h>

int main(void)
{
    const char *path = "private-travel-log.txt";

    /* TODO: write three records, close, reopen, display and remove the file. */
    (void)path;
    return 0;
}
