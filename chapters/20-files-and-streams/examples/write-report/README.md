# Write report

Run this example inside its own folder. Opening with `"w"` creates or replaces `journey.txt`.

```bash
make write-report
./write-report
```

The programme checks both formatted writes and `fclose`.
