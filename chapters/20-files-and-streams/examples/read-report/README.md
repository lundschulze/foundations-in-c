# Read report

Place a `journey.txt` practice file in this directory, then run:

```bash
make read-report
./read-report
```

The result of `fgets` controls the loop. `ferror` distinguishes a read error from normal end of file.
