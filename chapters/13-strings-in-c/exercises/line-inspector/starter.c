#include <stdbool.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    /* Read a line into an array with capacity 80. */
    /* Remove a retained newline and inspect the resulting string. */

    return 0;
}

