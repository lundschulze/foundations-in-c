# Uppercase first

The programme checks the first array element before changing it and leaves the null terminator intact.

```bash
make uppercase_first
./uppercase_first
```

Expected output:

```text
Maya
```

