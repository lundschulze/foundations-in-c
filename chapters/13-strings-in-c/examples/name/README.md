# Name

The six visible characters are followed by a null terminator, so the array contains seven elements.

```bash
make name
./name
```

Expected output:

```text
Traveller: Maya
```

For this array, `sizeof name` is 7 while `strlen(name)` would be 6.

