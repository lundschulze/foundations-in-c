# Calloc counters

`calloc` supplies storage with all bits initially zero. For these integer elements, every initial value is zero.

```bash
make calloc-counters
./calloc-counters
```

Expected output: `0`, `2`, `0` and `1` on separate lines.
