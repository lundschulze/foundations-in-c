# Allocate numbers

`main` owns the allocation, checks the result before use and releases it exactly once.

```bash
make allocate-numbers
./allocate-numbers
```

The programme prints the integers 1 through 5.
