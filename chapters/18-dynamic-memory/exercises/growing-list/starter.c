#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    size_t count = 2;
    int *values = NULL;

    /* TODO: allocate two integers, then grow the list to five. */
    (void)count;
    (void)values;
    return 0;
}
