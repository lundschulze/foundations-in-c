# Exercise — Make a growing list

Allocate room for two integers, fill them, grow the allocation to five and initialise the new elements. Keep the count separate, check the size multiplication and both allocation results, and release the final region exactly once.

```bash
make starter
./starter
make answer
./answer
```

The answer prints `10` through `50` in steps of ten.
