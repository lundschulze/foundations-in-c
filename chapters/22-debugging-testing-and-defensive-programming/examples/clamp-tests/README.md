# Clamp tests

These assertions cover a middle value, both boundaries and one value beyond each boundary.

```bash
make test
```

Assertions document programmer expectations. They are not a substitute for handling input or resource failures.
