# Total

This programme uses `0` as a sentinel and accumulates every preceding integer.

```bash
make total
./total
```

Enter `10`, `20` and `0` on successive prompts. The final line is:

```text
Total: 30
```

Try ending immediately with `0` and then try a word in a separate run.

