# Countdown

Trace the value of `count` through every pass before running the programme.

```bash
make countdown
./countdown
```

Expected output:

```text
3
2
1
Lift off.
```

Change the initial value to zero. Because `while` checks its condition before the first pass, only the final message is printed.

