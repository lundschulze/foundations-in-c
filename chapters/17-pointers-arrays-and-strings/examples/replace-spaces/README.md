# Replace spaces

The function requires writable character storage. The caller therefore supplies an array, not a string literal.

```bash
make replace-spaces
./replace-spaces
```

Expected output: `Guide_entry`.
