# Walk array

`end` is a valid one-past pointer used only as a boundary. The loop tests the boundary before dereferencing `current`.

```bash
make walk-array
./walk-array
```

The programme prints `8`, `13` and `21` on separate lines.
