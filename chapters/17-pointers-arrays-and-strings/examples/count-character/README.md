# Count character

The pointer advances until it reaches the null terminator. `const char *` makes the read-only contract visible.

```bash
make count-character
./count-character
```

Expected output:

```text
e: 3
R: 1
z: 0
```
