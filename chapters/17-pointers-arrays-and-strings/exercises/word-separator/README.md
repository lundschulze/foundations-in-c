# Exercise — Make a word separator

Complete the function so every underscore in a writable C string becomes a space. Test normal text, an empty string and text without underscores.

```bash
make starter
./starter
make answer
./answer
```

A string literal is not a valid argument because the function writes through its pointer.
