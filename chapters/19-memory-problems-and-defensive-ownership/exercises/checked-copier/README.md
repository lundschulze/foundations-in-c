# Exercise — Make a checked copier

Complete `copy_doubles`. It must copy only when the complete source fits and must leave the destination untouched otherwise.

```bash
make starter
make test
```

The answer tests exact, excess and insufficient capacity.
