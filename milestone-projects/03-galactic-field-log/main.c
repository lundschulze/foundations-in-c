#include <stdio.h>

int main(void)
{
    puts("Galactic Field Log");
    puts("Read README.md, then replace this placeholder with your own project.");

    /*
     * Suggested first steps:
     * 1. Design the record and collection types.
     * 2. Move collection operations into field_log.c and field_log.h.
     * 3. Add storage.c and storage.h only after memory ownership works.
     * 4. Keep the programme compiling after every small change.
     */

    return 0;
}
