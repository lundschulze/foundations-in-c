#include <stdio.h>

int main(void)
{
    puts("Guide Entry Catalogue");
    puts("Read README.md, then replace this starter with your own solution.");

    /*
     * Suggested first steps:
     * 1. Define the guide-entry structure and fixed-size array.
     * 2. Display the menu in a loop.
     * 3. Implement one operation at a time.
     * 4. Use a pointer when searching for and modifying an entry.
     */

    return 0;
}
