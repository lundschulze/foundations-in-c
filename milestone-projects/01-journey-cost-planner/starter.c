#include <stdio.h>

int main(void)
{
    puts("Journey Cost Planner");
    puts("Read README.md, then replace this starter with your own solution.");

    /*
     * Suggested first steps:
     * 1. Display the destination-zone menu.
     * 2. Read and validate the three selections.
     * 3. Move each calculation into a small function.
     * 4. Print and manually check the final quote.
     */

    return 0;
}
